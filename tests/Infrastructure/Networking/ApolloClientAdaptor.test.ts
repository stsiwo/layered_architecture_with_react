import { ApolloClientAdaptor } from "../../../src/Infrastructure/Networking/ApolloClientAdaptor";

test('requestQuery should return test data', () => {


});

test('requestQuery should return undefined because there is error', () => {
});
