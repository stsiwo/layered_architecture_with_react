export enum QueryTypeConstants {
    // query
    GetCartById,
    GetCartByUserId,

    // update (mutation)
    UpdateCart,
    UpdateOrder,

    // create (mutation)
    CreateCart,
    CreateOrder,

    // app state
    ToggleMenuRightSidebar,
}
