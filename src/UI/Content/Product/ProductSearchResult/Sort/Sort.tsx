import * as React from "react";
import "./Sort.scss";

const Sort: React.FunctionComponent<{}> = (props: {}) => {

    return (
        <div className="product-search-result-list-sort-wrapper">
           sort 
        </div>
    );
} 

export default Sort;
